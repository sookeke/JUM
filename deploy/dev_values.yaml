global:
  vanityURL: "jum-e27db1-dev.apps.gold.devops.gov.bc.ca"
  commit_sha: ""
  image:
    tag: develop
  environment: dev
  licenseplate: e27db1

notification:
  image:
    repository: jum-notification
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: "develop"